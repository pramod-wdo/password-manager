<h2>Sign in</h2>

<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="form-floating mb-3">
    <p><%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control", placeholder: "username@example.com" %></p>
    <p><%= f.label :email %></p>
  </div>

  <div class="form-floating mb-3">
    <p><%= f.password_field :password, autocomplete: "current-password", class: "form-control", placeholder: "password" %></p>
    <p><%= f.label :password %></p>
  </div>

  <% if devise_mapping.rememberable? %>
    <div class="field mb-3">
      <p><%= f.check_box :remember_me, class: "form-check-input" %></p>
      <p><%= f.label :remember_me, class: "form-check-label" %></p>
    </div>
  <% end %>

  <div class="actions mb-3">
    <%= f.submit "Sign in", class: "btn btn-primary" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
